{
  "name": "@meos/melog-connectors",
  "version": "0.2.0",
  "description": "MeOS 官方连接器：MeLog 条目 + 品牌渠道快照（零运行时依赖）",
  "type": "module",
  "bin": {
    "melog-connector": "./dist/cli.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.7",
    "typescript": "^5.3.3",
    "vitest": "^1.1.3"
  }
}
